// getUserMedia rear camera as the AR background layer. Requires HTTPS.
export async function start(videoEl) { throw new Error("TODO camera.start"); }
export function stop(videoEl) { throw new Error("TODO camera.stop"); }
