// Mode: framing — see docs/PLAN.md for the feature list this mode owns.
export function enter(state) { /* TODO framing.enter */ }
export function exit(state) { /* TODO framing.exit */ }
export function render(ctx, state) { /* TODO framing.render */ }
