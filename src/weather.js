// Open-Meteo (free, no key): cloud cover low/mid/high, humidity, wind, dew point,
// hourly for 7 days at the observer location. Cached per (lat,lon rounded) + hour.
// Seeing/transparency (Astrospheric's edge) is NOT available free — see PLAN.md.
export async function forecast(lat, lon) { throw new Error("TODO weather.forecast"); }
